/*

DECLARE @CurrencyCode VARCHAR(10) = '{0}' --'AUD'
DECLARE @ReferenceDate DATETIME = '{1}' --'2020-04-22'
DECLARE @ReferenceType VARCHAR(10) = '{2}' --'MONTHLY'

*/

SELECT @CurrencyCode, @ReferenceDate, @ReferenceType